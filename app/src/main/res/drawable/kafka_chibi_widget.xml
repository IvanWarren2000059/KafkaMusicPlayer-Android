<?xml version="1.0" encoding="utf-8"?>
<animated-vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt">
    
    <aapt:attr name="android:drawable">
        <vector
            android:width="64dp"
            android:height="64dp"
            android:viewportWidth="64"
            android:viewportHeight="64">
            
            <!-- This is a wrapper for the kafka_playing image -->
            <!-- The actual animation will be applied via scale -->
            <group android:name="kafkaChibiGroup"
                android:pivotX="32"
                android:pivotY="32">
                
                <!-- Placeholder path - the actual image will be shown via ImageView -->
                <!-- This just provides the animation framework -->
                <path
                    android:pathData="M32,32 m-30,0 a30,30 0,1,1 60,0 a30,30 0,1,1 -60,0"
                    android:fillColor="#00000000"/>
            </group>
        </vector>
    </aapt:attr>
    
    <!-- Pulsating scale animation - matching the main app -->
    <target android:name="kafkaChibiGroup">
        <aapt:attr name="android:animation">
            <set>
                <objectAnimator
                    android:propertyName="scaleX"
                    android:duration="1000"
                    android:valueFrom="1.0"
                    android:valueTo="1.1"
                    android:valueType="floatType"
                    android:repeatCount="infinite"
                    android:repeatMode="reverse"
                    android:interpolator="@android:anim/accelerate_decelerate_interpolator"/>
                <objectAnimator
                    android:propertyName="scaleY"
                    android:duration="1000"
                    android:valueFrom="1.0"
                    android:valueTo="1.1"
                    android:valueType="floatType"
                    android:repeatCount="infinite"
                    android:repeatMode="reverse"
                    android:interpolator="@android:anim/accelerate_decelerate_interpolator"/>
            </set>
        </aapt:attr>
    </target>
    
</animated-vector>
